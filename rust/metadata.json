{
  "language": "rust",
  "implementations": {
    "basic": {
      "dir": "basic",
      "build": "cargo build --release --target-dir bin",
      "run": "bin/release/sudoku"
    },
    "optimised": {
      "dir": "optimised",
      "build": "cargo build --release --target-dir bin",
      "run": "bin/release/sudoku"
    },
    "optimised-multithreaded": {
      "dir": "optimised-multithread",
      "build": "cargo build --release --target-dir bin",
      "run": "bin/release/sudoku"
    },
    "optimised-cleaned": {
      "dir": "optimised-cleaned",
      "build": "cargo build --release --target-dir bin",
      "run": "bin/release/sudoku"
    }
  }
}